{
  "id": "@weaver/plugin-bitbucket",
  "name": "Bitbucket Integration",
  "version": "1.0.0",
  "description": "Link Bitbucket commits, pull requests, and branches to Weaver issues",
  "author": "Weaver Team",
  "icon": "folder-git-2",
  "type": "integration",
  "entrypoints": {
    "server": "./src/server/index.ts",
    "client": "./src/client/index.ts"
  },
  "permissions": ["issues:read", "issues:write", "webhooks:manage"],
  "settings": {
    "schema": {
      "webhookSecret": { "type": "string", "required": true, "description": "Secret token included in the Bitbucket webhook URL" }
    }
  },
  "events": {
    "subscribes": ["issue.created", "issue.status_changed"],
    "emits": ["bitbucket.push_received", "bitbucket.pr_opened", "bitbucket.pr_merged"]
  },
  "ui": {
    "slots": [
      { "slot": "issue-detail-panel", "component": "BitbucketPanel" }
    ]
  },
  "routes": [
    { "method": "POST", "path": "/webhook/:tenantId/:token", "handler": "handleBitbucketWebhook", "public": true },
    { "method": "GET", "path": "/links/:issueKey", "handler": "getBitbucketLinks", "requiredPermissions": ["issues.read"] }
  ],
  "migrations": ["./migrations/001_create_bitbucket_links.sql"]
}
