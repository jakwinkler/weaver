apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: weaver
  namespace: weaver
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - weaver.example.com
      secretName: weaver-tls
